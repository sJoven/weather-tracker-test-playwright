{
  "name": "weather-tracker-playwright-tests",
  "version": "1.0.0",
  "description": "Playwright tests for the Weather Tracker App",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "show-report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "engines": {
    "node": ">=18"
  }
}